import { CambiartextoPipe } from './cambiartexto.pipe';

describe('CambiartextoPipe', () => {
  it('create an instance', () => {
    const pipe = new CambiartextoPipe();
    expect(pipe).toBeTruthy();
  });
});
